---
const { title, desc, tech, lang = 'zh' } = Astro.props;
const isEn = lang === 'en';
---

<div class="single-item">
  <h3>{title}</h3>
  <div class="project-desc project-desc-list w-richtext">
    <ul role="list">
      {desc.map((item: string) => <li>{item}</li>)}
    </ul>
  </div>
  <div class="mini-label-white">{isEn ? 'Technologies & tools' : '技术栈 & 工具'}</div>
  <div class="chips-container">
    {tech.map((techItem: string) => <div class="single-chip">{techItem}</div>)}
  </div>
</div>
